<div class=" navbar navbar-expand-lg navbar-dark bg-white rounded">
    <div class="col-md-12 ml-4 d-flex justify-content-around align-items-baseline">
        {# liens de pied de page #}
        <h6 class=" p-2"><a href="#" class="mr-4">Conditions d'utilisation</a>   <a class="mr-4" href="#">Politique de
                Confidentialité</a> <a href="{{ path('contact') }}">Contact</a></h6>

        <div class="float-left"> © Kids  &  Family 2021</div>
        <div>
            {%   if is_granted('IS_AUTHENTICATED_FULLY') %}
                <a href="{{ path('activity_new') }}" class="btn btn-outline-success "><i class="fas fa-plus-circle"></i></a>
            {% endif %}
        </div>


        <div class="justify-content-space-between">
            <a href="#" class="btn btn-outline-warning"><i class="fab fa-instagram"></i></a>
            <a href="#" class="btn btn-outline-primary"><i class="fab fa-facebook-square"></i></a>
            <a href="#" class="btn btn-outline-danger"><i class="fab fa-youtube-square"></i></a>
        </div>

    </div>
</div>
