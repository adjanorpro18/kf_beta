

<div>
    <form method="post" action="{{ path('activity_delete', {'id': activity.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer?');">
        <input type="hidden" name="_method" value="DELETE">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activity.id) }}">
        <button class="btn btn-md btn-outline-danger float-right m-0"><i class="fas fa-trash-alt"></i></button>
    </form>
</div>
