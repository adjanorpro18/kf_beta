<div class="container">
    {{ form_start (form) }}
    <div class="mb-5">
        <h6 class="mb-2">Recherche</h6>
        {{ form_widget(form.q) }}
    </div>
    <div class="mb-5">
        <h6 class="mb-2">Catégories</h6>
        {{ form_widget(form.categories) }}
    </div>

    <div class="mb-5">
        <h6 class="mb-2"> Date minimum</h6>
        {{ form_widget(form.min) }}
    </div>

    <button type="submit" class="btn btn-primary w-100 mt-3"> Filtrer</button>

    {{ form_end (form) }}
</div>