
<div class="container-fluid m-2 p-2 justify-content-center rounded">
    <div class="mb-4">
        {{ form_start(form) }}
        {{ form_widget(form) }}
    </div>
    <div class="d-flex justify-content-end ">
        <button class="btn btn-lg btn-primary" type="submit">{{ button_label|default('Enregistrer') }}</button>

    </div>

    {{ form_end(form) }}

</div>
